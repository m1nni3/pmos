[build]
  base    = "apps/web"
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  targetPort = 5173

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[functions]
  directory = "../../netlify/functions"
